# 应用使用说明

## ✅ 应用概述

Toolbox 是一个多功能桌面生产力应用，集成了 Wiki 知识库和提醒功能。

## 🎯 应用功能

这个 Tauri 应用包含两个主要功能：

### 1. 📚 Wiki 知识库（主界面）
强大的个人知识管理系统，包括：

**编辑功能**
- **Markdown 编辑器**：支持完整的 Markdown 语法
- **WYSIWYG 模式**：所见即所得的富文本编辑
- **实时预览**：左右分栏实时预览
- **代码高亮**：支持多种编程语言的语法高亮

**图表支持**
- **Mermaid 图表**：流程图、序列图、类图、饼图等
- **PlantUML 图表**：UML 图、用例图、活动图等

**组织管理**
- **分类管理**：层级分类组织页面
- **标签系统**：灵活的标签分类
- **版本历史**：自动保存修订历史，支持恢复
- **全文搜索**：快速搜索所有页面

**导入导出**
- **文档导入**：支持导入 Word 等外部文档
- **表格插入**：快速创建 Markdown 表格

### 2. 📝 Reminders 提醒应用（抽屉式面板）
完整的提醒管理系统，通过 Wiki 界面的按钮打开：

**Smart Lists（智能列表）**
- **Today**：今天的提醒
- **Scheduled**：所有已计划的提醒
- **Flagged**：已标记的重要提醒
- **All**：所有提醒

**自定义分类**
- Work（工作）
- Personal（个人）
- Shopping（购物）
- Health（健康）
- Other（其他）
- 可以添加更多自定义分类

**功能特性**
- 添加/编辑/删除提醒
- 设置提醒时间
- 标记完成状态
- 重要事项标记（旗帜）
- 重复提醒（一次、每天、每周、每月、每年）
- 详细信息面板
- 文件附件管理
- GitHub 云同步

### 3. 🔔 通知系统
- 独立通知窗口
- 每 30 秒检查到期提醒
- 快速操作：完成或延后
- 与主窗口实时同步

## 🚀 启动应用

```bash
cd /path/to/toolbox
npm install
npm run tauri dev
```

或使用便捷脚本：
```bash
./run.sh
```

## 💡 使用方法

### Wiki 使用
1. **新建页面**：点击顶部 "+ New Page" 按钮
2. **编辑页面**：在编辑器中输入 Markdown 内容
3. **切换模式**：使用工具栏切换 Markdown/WYSIWYG 模式
4. **保存页面**：自动保存或点击 "Save" 按钮
5. **管理分类**：右键点击侧边栏分类进行操作

### Reminders 使用
1. **打开面板**：点击顶部 "📝 Reminders" 按钮
2. **添加提醒**：在提醒列表顶部输入标题，按 Enter
3. **编辑提醒**：双击提醒打开详情面板
4. **完成提醒**：点击圆圈标记完成
5. **标记重要**：点击旗帜图标

### GitHub 同步设置
1. 点击 "⚙️ Sync Settings"
2. 启用同步并选择 GitHub 作为数据源
3. 生成 GitHub Token 并输入
4. 测试连接后保存设置

## 📁 项目结构

```
src/
├── AppSimple.vue           # 入口组件（渲染 WikiApp）
├── main.ts                 # Vue 启动入口
└── components/
    ├── WikiApp.vue         # 主容器 + Reminders 抽屉
    ├── WikiEditor.vue      # Markdown/WYSIWYG 编辑器
    ├── WikiPreview.vue     # 实时预览组件
    ├── WikiSidebar.vue     # 侧边栏（分类、页面列表）
    ├── WikiMetadata.vue    # 元数据面板（标签、历史）
    ├── RemindersApp.vue    # 提醒应用组件
    ├── DocumentImportModal.vue  # 文档导入
    ├── TableInsertModal.vue     # 表格插入
    ├── ReminderInsertModal.vue  # 提醒插入
    ├── ContextMenu.vue     # 右键菜单
    └── SectionNode.vue     # 递归分类节点

src-tauri/
└── src/
    ├── lib.rs              # 主入口和命令注册
    ├── main.rs             # 应用入口
    ├── commands/           # 提醒命令
    ├── database/           # 数据库操作
    ├── models/             # 数据模型
    ├── notifications/      # 通知服务
    ├── sync/               # GitHub 同步
    ├── tray/               # 系统托盘
    ├── wiki_commands.rs    # Wiki 命令
    ├── evidence_commands.rs # 附件命令
    └── sync_commands.rs    # 同步命令
```

## 🔧 技术栈

- **Frontend**: Vue 3 + TypeScript + Vite
- **Backend**: Rust + Tauri 2
- **Database**: SQLite（提醒数据）+ JSON 文件（Wiki 数据）
- **Styling**: CSS3（原生样式）+ 深色模式支持
- **Diagrams**: Mermaid + PlantUML

## 📝 数据存储

### 存储位置
- **macOS**: `~/Library/Application Support/com.yaozhuang.tauri-vue-app/`
- **Linux**: `~/.local/share/com.yaozhuang.tauri-vue-app/`
- **Windows**: `%APPDATA%\com.yaozhuang.tauri-vue-app\`

### 数据结构
- `reminders.db` - SQLite 数据库（提醒和附件）
- `wiki/pages/` - Wiki 页面 JSON 文件
- `wiki/revisions/` - 页面修订历史
- `wiki/sections.json` - 分类结构

## ❓ 常见问题

**Q: 如何访问提醒功能？**
A: 点击 Wiki 顶栏的 "📝 Reminders" 按钮打开抽屉式面板。

**Q: 如何添加新的 Wiki 页面？**
A: 点击顶部的 "+ New Page" 按钮，或在侧边栏右键选择分类后添加。

**Q: 支持哪些 Markdown 功能？**
A: 支持完整的 GFM（GitHub Flavored Markdown），包括表格、代码块、任务列表等。

**Q: 如何插入图表？**
A: 使用 \`\`\`mermaid 或 \`\`\`plantuml 代码块插入图表。

**Q: 如何启用深色模式？**
A: 应用自动跟随系统主题设置。

**Q: 数据如何备份？**
A: 可以使用 GitHub 同步功能备份提醒数据，Wiki 数据存储在本地文件系统中。

## 🎉 享受使用！

现在你有一个集成了 Wiki 知识库和提醒管理的强大桌面应用！
